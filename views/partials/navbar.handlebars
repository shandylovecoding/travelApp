<nav class="navbar navbar-expand-lg navbar-light lighten-4 ">
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="logo">
                <a class="navbar-brand" href="/">
                    <h2>TripPlanner</h2>
                </a>
            </div>
            <div class="searcher">
                <form id="search_form" class="searchform" role="search" method="get" action="/search">
                    <i class="fa fa-search"></i>
                    <input type="text" class="searcher typeahead" id="searcher" name="title" value=""
                        placeholder="Search tour by keywords" autocomplete="off">
                    <div id="menu_search_autocomplete" class="autocomplete" data-mousedown="false"></div>
                </form>
            </div>
            {{#if username}}
            <ul class="navbar-nav">
                    <li class="nav-item">
                        <a href="/profile/{{username}}" class="nav-link"><p>Profile</p> </a>
                    </li>
                    <li class="nav-item">
                        <a href="/logout" class="nav-link">Logout</a>
                    </li>
                </ul>
            {{else}}
            <div class="collapse navbar-collapse  d-flex justify-content-around" id="navbarNav">
                <ul class="navbar-nav">
                    <!--If logged out the profile link will redirect to the login page-->
                    <li class="nav-item">
                        <a class="nav-link" href="#fLogin" data-toggle="modal">Profile</a>
                    </li>
                    <!--Separation for possible removal? Keep if it's ok, delete if it doesn't work-->
                    <li class="nav-item">
                        <a class="nav-link" href="/tripsHome" >Trips</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/journals" >Journals</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" data-target="#fLogin" data-toggle="modal">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" data-target="#fsignUp" data-toggle="modal">Sign up</a>
                    </li>
                </ul>
            </div>
            {{/if}}

        </nav>